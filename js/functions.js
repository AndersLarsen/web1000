/************************************************Validering av Epost registrer.php**************************************************///function validate(registrerForm,email) {//	 //	   var pattern = new RegExp (" /^([A-Za-z0-9_\-\.])+\@([A-Za-z0-9_\-\.])+\.([A-Za-z]{2,4})$/ ");//	   var address = document.forms[form_id].elements[email].value;//	   if(pattern.test(address) == false) {//	 //	      alert('Feil utfylt epost felt');//	      return false;//	   }//	}/*****************************************function bekreft*****************************************/function confirmDelete(){        /*	Hensikt			Funksjonen ber brukeren bekrefte valg		*/    if(confirm("Er du sikker at du vil slette ?" )){        return true;    }    else{        return false;    }}       /*Slutt function confirmDelete*//*****************************************function NoDelete*****************************************/function  NoDelete(){alert("Du har ikke valgt noen å slette...");}		/*Slutt function NoDelete*//****************************************************************************************************Validering av brukerinput vedrørende registrering/endring av en bruker*****************************************************************************************************//* Validering av input ny bruker */function validateUsername(username) {						//VALIDERER BRUKERNAVN	/*	Hensikt	 		Funksjonen sjekker om username er korrekt fylt ut		Parametre			username = username som skal sjekkes		Funksjonsverdi			Funksjonen returnerer true hvis korrekt fylt ut			Funksjonen returnerer false ellers	*/	var pattern = new RegExp ( "[A-Za-z0-9]{3,30}" );	if(pattern.test(username)){					// sjekker om parameter validerer mot mønster		return true;							// returnerer "true" hvis det stemmer	}	else{		return false;							// returnerer "false" hvis det ikke stemmer	}	}function validateEmail(email) {					//VALIDERER EMAIL (HAR HJELP OGSÅ GJENNOM HTML5 STANDAREN)	/*	Hensikt	 		Funksjonen sjekker email er korrekt fylt ut		Parametre			email = email som skal sjekkes		Funksjonsverdi			Funksjonen returnerer true hvis korrekt fylt ut			Funksjonen returnerer false ellers	*/	   var pattern = "^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,4}$";	   	   if(pattern.test(email)){						// sjekker om parameter validerer mot mønster			return true;							// returnerer "true" hvis det stemmer		}		else{			return false;							// returnerer "false" hvis det ikke stemmer		}	      	   	   }function validatePassword(password) {					//VALIDERER AT PASSORD ER FYLT INN	/*	Hensikt			 Funksjonen sjekker password er korrekt fylt ut		Parametre			password = password som skal sjekkes		Funksjonsverdi			Funksjonen returnerer true hvis korrekt fylt ut			Funksjonen returnerer false ellers	*/	var pattern = new RegExp ( ".{6,}" );	if(pattern.test(password)){					// sjekker om parameter validerer mot mønster		return true;							// returnerer "true" hvis det stemmer	}	else{		return false;							// returnerer "false" hvis det ikke stemmer	}		}/* Validering av registrering av ny student */function validatorStudent(username,password) {	/* 	Hensikt	 		Funksjonen sjekker om feltene i registrering er korrekt fylt ut		Parametre			username = username som skal sjekkes			email = email som skal sjekkes			password = password som skal sjekkes		Funksjonsverdi			Funksjonen returnerer true hvis feltene er korrekt fylt ut			Funksjonen returnerer false ellers	*/	var legalUsername = validateUsername(username);	var legalPassword = validatePassword(password);	var feilmelding="";		if(!legalUsername){		feilmelding="Brukernavn er ikke korrekt fylt ut"+"\n";	}	if(!legalPassword) {		feilmelding=feilmelding + "Passord er ikke korrekt fylt ut"+"\n";	}	if (legalUsername && legalPassword) {		return true;	} else {		alert(feilmelding);		return false;	}}/* Validering av registrering av ny student */function validatorUser(username) {	/* 	Hensikt	 		Funksjonen sjekker om feltene i registrering er korrekt fylt ut		Parametre			username = username som skal sjekkes		Funksjonsverdi			Funksjonen returnerer true hvis feltene er korrekt fylt ut			Funksjonen returnerer false ellers	*/	var legalUsername = validateUsername(username);	var feilmelding="";		if(!legalUsername){		feilmelding= "Brukernavn er ikke korrekt fylt ut";	}	if (legalUsername) {		return true;	} else {		alert(feilmelding);		return false;	}}/************************Validering rom*********************************/function validateRoomName(room_name) {	/* Hensikt	 * 		Funksjonen validerer at romnavn er fylt ut korrekt. 	* Parametre	* 		$room_name: romnavnet som skal valideres	* Returnerer	* 		"true" eller alertmelding	*/		var pattern = new RegExp("[A-Z][1-9][-][0-9]{1,2}[0-9A-Z]{0,2}");	// mønster som brukes av funksjonen preg_match for å validere input				if(pattern.test(room_name)){		// sjekker om parameter validerer mot mønster			return true;							// returnerer "true" hvis det stemmer		}		else{			alert("Romnavn er ikke korrekt utfylt. Skal ha format [Blokk][Etasje]-[Romnummer]");	// returnerer alertmelding hvis det ikke stemmer			return false;		}	}/*End function validateRoomName*/function validateRoomType(room_type) {	/* Hensikt	 * 		Funksjonen validerer at romnavn er fylt ut korrekt	* Parametre	* 		$room_type: romtypenavn som skal valideres	* Returnerer	* 		"true" eller "false"	*/	var pattern = new RegExp("^[A-ZÆØÅ][a-z æøå]+$");			// mønster som brukes av funksjonen preg_match for å validere input		if(pattern.test(room_type)){		// sjekker om parameter validerer mot mønster		return true;							// returnerer "true" hvis det stemmer	}	else{		alert("Romtype er ikke korrekt utfylt. Skal starte med stor forbokstav og kun bestå av bokstaver");		// returnerer alertmelding hvis det ikke stemmer		return false;	}}/*End function validateRoomType*/